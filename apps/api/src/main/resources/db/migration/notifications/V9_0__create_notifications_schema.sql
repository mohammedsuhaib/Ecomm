-- notifications module schema (schema-per-module rule).
-- Owns: notification_log (core). push_subscriptions arrives with the Web Push add-on. Fleshed out in M3.
CREATE SCHEMA IF NOT EXISTS notifications;
